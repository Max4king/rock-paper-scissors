//let playerSelection = parseInt(prompt("Choose from 0 to 2: "));
const choice = ["rock","paper","scissors"];

function getComputerChoice() {
    return (Math.floor(Math.random() * 10)%3);
}

function winner_loser(player, computer) {

}
